# 稳定性修复记录（2026-09-10）

基于9ba4378继续修复，本次仅本地修改，不制作安装包、不发布GitHub。旧审查报告属于历史快照；当前点位及协议说明见红线复核.md和PLC点位表.md。

## 已实施

- 数值解析拒绝非零文本下溢成零；保留真实零、科学计数法、负值支持，不增加量程限制。
- 用户关闭统一确认，取消后保留窗口及定时器；系统关闭不要求人工确认。
- 从站异常及本地参数错误不再盲目断开并重试；IO、Socket、超时故障仍断开。
- 耐压日期、批次、结果及数据缺失给出点位诊断；匹配失败显示批次、日期、产品。
- 业务故障和ACK协调重复告警按30秒限流，业务重试仍按原周期进行；成功恢复后清除告警限流状态并记录恢复。
- 产品坏图30秒退避，文件修改后可立即重新加载。
- 产品下拉异步查询product_catalog后展开，保留当前文本；避免在消息处理中同步查库。
- 备份目录枚举和逐文件删除均独立处理异常，不将已完成的有效备份误报失败；临时文件清理异常不覆盖原始错误。
- 更新旧红线复核文档；PLC地址、DINT类型、ACK及历史数据规则不变。

## 验证

- Release构建成功，0警告、0错误。
- 97项单元/集成测试通过，包括科学计数法下溢不入库不应答、重复告警、耐压诊断、通信异常分类，以及原有SQLite备份和ACK恢复测试。
- UI脚本验证取消退出、异步刷新后出现新增产品、日志暂停/恢复、指导书完整显示与CSV规则。
- 1920×1080、1600×900、1280×720顶层布局边界检查通过。
- 检查未启动PLC服务，通信分类测试未建立网络连接；不等同于现场PLC、长期运行或高DPI专项验证。

## 保持待确认

员工号是否仅允许正数或固定六位、测量量程、非午夜批次重置、同批重测规则不在本次修改范围。深分页与五年数据量压测另行安排；未修改生产库和已有安装目录。

## 第二轮边界与资源加固

- ToModbus使用long计算并校验范围；读寄存器数量1～125、写多个寄存器数量1～123，并校验末寄存器不超过65535。非法请求在连接前拒绝，不更改有效点位映射。
- TCP连接改用ConnectAsync，限时等待；超时关闭临时socket，并观察异步任务迟到异常，不再遗留BeginConnect未收尾路径。
- 主窗体业务事件使用具名处理器，Dispose时完整退订；主窗体与指导书独立创建的字体显式释放，不释放共享主题字体。
- 指导书无图片分支先解除引用再释放旧图，作为未来动态切换的防御性保护。
- 当前108项测试全部通过；包含边界输入在联网前失败、SQLite写锁期间健康检查失败且释放锁后恢复。未额外设置busy_timeout，现有超时机制通过此用例。
- Release构建0警告0错误；UI取消退出、异步目录刷新、三种分辨率布局检查通过。
- 本次UI测试进程实际DPI为96×96，仅覆盖100%缩放。125%、150%、200%及跨屏DPI切换仍待对应环境验证；没有调整用户系统缩放，也没有据此声称高DPI已全部通过。
- SafeInvoke保留现有父类异常捕获（已覆盖ObjectDisposedException）；未改变PLC点位、ACK或其他业务规则。本轮仍不发布。
