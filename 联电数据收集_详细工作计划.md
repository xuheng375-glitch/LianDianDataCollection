# 联电数据收集——更新与交付工作计划

## 2026-09-08 深度优化基线（优先于所有历史章节）

- 用户产量：约 2,000 条/天；五年按 3,654,000 条进行容量验证。历史主库永久保留，不启用五年前自动删除。
- 已完成：SQLite 引擎升级 3.53.3 / ADO.NET 2.0.4，固定 Windows x64，WAL + FULL，在线备份及校验、磁盘余量健康检查、有界日志。
- 已完成：定时器串行与关闭等待；快照有效期、读取失败字段失效、重连周期作废、二次确认握手标志；事务持久化待应答信息，启动可恢复未确认批次。
- 已完成：主界面/指导书/退出弹窗统一主题；图片完整适配；产品下拉展开前读取数据库；分页查询和流式导出；三种分辨率离屏布局复核。
- 已完成：D4030 完整 DINT 支持 600249；D4000 仅写；D4002=1 时读取 D6000；当前地址偏移 1。
- 验证记录与容量结果统一见 `docs/深度检测报告-2026-09-08.md`，下方 30/35 条和旧驱动版本均为历史记录。
- 交付：按用户要求新建私有仓库 `xuheng375-glitch/LianDianDataCollection`，只上传源码、测试和文档，不上传生产库/日志/产品资料。
- 未完成的上线门槛：真实 PLC 全链路、响应丢失/掉电恢复、168 小时持续运行、125%/150% 缩放实机视觉检查、异机备份还原。不得以短时自动测试代替。
- 现场握手要求：数据先写、最后置 1，并保持数据直至收到 2/3；必须让上位机观察到非 1 状态后再开始新请求。现有点表没有唯一请求序号，断电边界无法数学保证物理端到端恰好一次，需 PLC 工程师共同验收。

## 2026-09-07 最新点位变更（优先于下方历史基线）

员工工号补充修正：地址保持 D4030，按完整 DINT 读取 D4030＋D4031，替代此前仅读低 WORD 的约定；界面与批次入库均支持 600249，待 AutoShop 现场确认。地址偏移现为 1，D 号对应同号协议地址。

- 已完成：按用户最新点位表迁移 PLC 地址；D4100/D4200/D4300 为五位批次号 STRING，D5700/D5800/D5900 为 yyDDD STRING；D4030 仅读低 WORD；D4000 仅写心跳。
- 已完成：D4002＝1 时直接读取 D6000 单个大写 A～F，保存到批次记录的 `qr_grade`；SQLite 启动时自动补列，原有记录不删除；查询表增加二维码等级，批次号显示五位。
- 已验证：35/35 单元与数据层测试通过；实际 PLC 联调待用户现场测试。按用户要求，已删除模拟从站工具、编译产物和模拟日志，后续仅进行真实 PLC 联调。
- 待现场确认（最高优先级）：STRING 是否无长度头、字节序、占用寄存器数及 ModbusOffset。当前批次/日期默认 3 个寄存器，等级默认 1 个；参数位于 `config/app.ini`。模拟测试不等同于真实 PLC 验收。
- 后续：先做小范围新点位读写验收，再做异常重连与持续跑批；SunnyUI 实机风格、布局和操作习惯验收仍未完成，不以编译通过代替。
- 最新地址以 `docs/PLC点位表.md` 和 `src/LianDian.Comm/RegisterMap.cs` 为准；下方旧 30 条测试基线和旧协议描述仅供历史参考。

> 复核日期：2026-08-27
> 复核依据：当前源码、配置、测试、联调日志与交付文档
> 本轮变更目标：同步 GitHub PLC 程序、MySQL 全量迁移至 SQLite、UI 全面统一 SunnyUI

## 1. 当前结论

核心业务代码已完成，单元/数据层测试已通过；实际 PLC 通信和 SunnyUI 界面尚待现场验收，不能宣告最终交付完成。

本轮已完成 SQLite 与 SunnyUI 的代码迁移；UI 仍需进行 1920×1080 实机视觉验收。PLC 已锁定 GitHub 目标提交并同步通信可靠性改进，最新点位已按用户要求更新，地址基准差异等待实机确认。

## 2. 完成情况复核

| 模块 | 当前状态 | 证据/说明 | 后续动作 |
|---|---|---|---|
| 工程骨架与分层 | ✅ 完成 | Core/Comm/Data/Business/UI 分层可编译 | 保持 |
| 核心业务规则 | ✅ 完成 | 心跳、批次、耐压、气压；30/30 测试通过 | 现场 PLC 后回归 |
| PLC Modbus TCP 同步 | 🔄 部分完成 | 已锁定 `xuheng375-glitch/AirtightInspection@de71741`；低字序/低字节序/应答语义一致，已同步 3 次写入重试 | 地址基准差异需现场确认 |
| 寄存器红线 | ✅ 当前未改 | `RegisterMap.cs` 仍为唯一事实来源 | 若 GitHub 点位不同，先形成差异表并确认 |
| SQLite 数据层 | ✅ 代码与联调完成 | `System.Data.SQLite.Core`、单文件、WAL、建表/补列、参数化 SQL | 备份恢复与 7×24 测试 |
| MySQL 删除 | ✅ 源码完成 | 已移除 MySqlConnector、账号/端口配置及 MySQL 数据访问代码 | 清理历史报告措辞；不迁移旧 MySQL 数据，除非另行要求 |
| SunnyUI 控件统一 | ✅ 代码完成 | 主/子窗体为 `UIForm`；面板、灯、按钮、标签、图片、日期、下拉、表格均为 SunnyUI 控件 | 实机截图与操作习惯复核 |
| Release 构建 | ✅ 通过 | 0 警告、0 错误 | 最终变更后再跑一次 |
| 单元与数据层测试 | ✅ 通过 | 30/30（含 3 条真实 SQLite 仓储测试） | 最终变更后回归 |
| P6 实际 PLC 联调 | ⏳ 待现场测试 | 不沿用模拟结果作为验收依据 | 用户提供真实 PLC 测试结果 |
| 7×24 稳定性 | ⏳ 未完成 | 现有仅短时跑批，完整 7×24 仍无有效记录 | 在不休眠工控机执行 |
| 现场校准 | ⏳ 未完成 | Offset/WordOrder/字节序/编码未签字 | 现场执行并固化 |
| 文档 | ✅ 本轮已更新 | 工作计划、README、部署/操作说明、联调报告已同步 | 现场与 7×24 后终审 |

## 3. 硬性约束

1. `RegisterMap.cs` 是当前点位唯一事实来源。GitHub 程序与当前点位不一致时，先输出“地址、类型、长度、读写方向、业务含义”差异表，未经确认不得改地址。
2. 保持标志位 1/2/3、心跳 0/1、三方比对、同批次 UPDATE 等业务语义不变。
3. 数据库仅允许 SQLite；不得重新引入 MySQL 服务、驱动、连接串或部署依赖。
4. UI 可视控件必须来自 SunnyUI。允许使用 WinForms 基础类型作为事件参数、布局枚举和 SunnyUI 的基类 API，不允许新增原生业务控件或自研替代控件。
5. 跨线程 UI 更新使用 `BeginInvoke`；Timer、Socket、数据库连接与图片资源必须释放。

## 4. 优化后的执行计划

### R1 PLC 通信同步（部分完成，地址确认阻塞）

已锁定来源：`xuheng375-glitch/AirtightInspection`，默认分支 `main`，目标提交 `de71741dad5a107709af0f2e5e4bac907e91e277`。

已完成差异核对：

- DINT 默认低字在前：一致。
- 字符串每寄存器 2 字符、低字节在前、无长度头：一致。
- PLC 先写数据最后置等待标志；上位机成功写 2、失败写 3；持续为 1 不重复处理：一致。
- 已同步目标提交中的 PLC 应答写入可靠性：最多 3 次尝试、失败断开后重连、尝试间隔可配置。
- 地址基准不一致：GitHub 说明 D4000 对应协议地址 4000；当前项目红线公式为 `D - 1 + ModbusOffset`。本轮未擅自改地址，必须通过实际 PLC 或 PLC 工程师确认。

1. ✅ 读取目标版本并记录提交号，禁止以仓库默认分支漂移版本作为验收依据。
2. 对比连接协议、站号、地址换算、寄存器点表、数据类型、字符串长度、字节序、超时、重试和并发模型。
3. 先生成差异表；区分“可直接同步”“配置化处理”“触及红线需确认”三类。
4. 在 `IPlcClient` 边界内完成兼容，尽量不让业务层感知通信驱动差异。
5. 补充协议编码、地址换算、掉线恢复、读写超时、边界长度测试。

出口：目标 GitHub 提交可追溯；差异全部闭环；点位变更有书面确认；通信与业务测试全绿。

### R2 SQLite 数据层（代码完成，预计 0.5～1 天回归）

已完成：

- 使用 `System.Data.SQLite.Core 1.0.119` 替换 MySqlConnector。
- 配置简化为 `FilePath=Data\lian_dian.db` 与健康检查周期。
- 首次启动自动建目录、数据库文件和 `batch_record` 表。
- 使用 WAL、Normal 同步、外键与连接池；全部 SQL 参数化。
- 唯一键冲突转换为统一 `DuplicateKeyException`。
- SQLite 单元测试使用临时数据库文件，不启停数据库服务。

待验证：

1. ✅ 已增加真实 SQLite 仓储测试：首次建库、重复启动、唯一键、查询筛选与 decimal 往返。
2. 在实际 PLC 上验证批次下发、耐压上传、气压上传与数据库关联。
3. 验证数据库不可访问、磁盘只读/满、文件损坏后的日志和心跳门控。
4. 验证退出后单文件备份与恢复；明确运行中不得只复制主文件而忽略 WAL。
5. 若需要保留旧 MySQL 生产数据，另做一次性迁移工具；当前需求仅删除 MySQL，不默认搬迁历史库。

出口：代码/配置/文档无 MySQL 运行依赖；仓储与联调测试通过；备份恢复步骤验证完成。

### R3 SunnyUI 工业 UI（代码完成，预计 1 天实机验收）

已完成：

- `MainForm`、`InstructionForm` 改为 `UIForm`。
- 状态灯使用 `UILedBulb`；产品图和指导书图片使用 `UIImageButton`。
- 面板使用 `UIPanel`；元信息面板不再继承原生 Panel。
- 保留 `UIButton`、`UILabel`、`UIComboBox`、`UIDatePicker`、`UIDataGridView`。
- 删除 `FlatPanel`、`FlatStatusLamp`、`ProductImageBox` 自研控件。

实机验收重点：

1. 1920×1080 与 125%/150% DPI 下无裁切、重叠、字体模糊。
2. PLC/数据库状态必须一眼可辨；异常信息颜色明确但不闪烁干扰操作。
3. 查询条件从左到右符合操作顺序；按钮热区、禁用态、悬停态一致。
4. 表格行高、列宽、时间、数值单位、OK/NG 色彩符合工业软件扫描习惯。
5. 指导书上一页/下一页/缩放/拖动/关闭可单手连续操作。
6. 键盘焦点、Tab 顺序、确认/退出行为防误触；退出需要二次确认（如现场要求）。
7. 用最新实机截图替换旧 UI 证据，旧 HTML 仅保留为原型参考。

出口：无原生/自研业务控件残留；代码扫描通过；实机验收签字；截图归档。

### R4 全量回归与稳定性（预计 2 天 + 7×24）

| 编号 | 验证项 | 通过标准 |
|---|---|---|
| G01 | Release 构建 | 0 警告、0 错误 |
| G02 | 单元/数据层测试 | 全部通过，新增 SQLite 测试有覆盖 |
| G03 | 实际 PLC 主流程 | 批次、耐压、气压、查询全部通过 |
| G04 | 现场异常验证 | 在安全测试窗口验证断线、超时、重复上传和数据库不可用 |
| G05 | 连续 500 批 | 零丢失、零错位、库内行数一致 |
| G06 | UI 实机 | SunnyUI 统一、DPI/交互/异常态通过 |
| G07 | 7×24 | 无未处理异常、无资源增长趋势、数据零丢失 |
| G08 | 现场 PLC | GitHub 版本通信一致，四项校准值固化 |

架构变化后，旧 MySQL 联调报告只作为历史证据，不计入新基线最终验收。

### R5 文档与交付（预计 0.5 天）

1. 更新 README、部署手册、操作说明、联调报告和红线复核。
2. 记录 SQLite 文件位置、备份/恢复、损坏排查和磁盘空间要求。
3. 记录 SunnyUI 版本与 GPL-3.0 许可证评估结果；商业交付前由项目方确认合规。
4. 记录 GitHub 仓库、目标提交号、通信差异与确认人。
5. 交付源码、配置模板、测试报告、现场校准记录与最终截图。

## 5. 优先级与排期

| 优先级 | 工作 | 前置 | 建议时间 |
|---|---|---|---|
| P0 | 确认 PLC 协议地址基准（D4000→3999 或 4000） | 实机/PLC 工程师 | 立即 |
| P0 | 实际 PLC 与 SQLite 主流程验收 | 现场设备与测试窗口 | 按现场安排 |
| P0 | SunnyUI 实机 UI 验收 | 可运行工控机/桌面 | 1 天 |
| P1 | PLC 差异合并与测试 | 仓库版本锁定 | 1～2 天 |
| P1 | 500 批与 7×24 | 前述回归全绿 | 2 天 + 7×24 |
| P1 | 现场校准 | PLC/网络窗口 | 1 天 |
| P2 | 文档终审与交付 | 新证据齐备 | 0.5 天 |

## 6. 当前阻塞与风险

| 风险/阻塞 | 影响 | 处理 |
|---|---|---|
| GitHub 与当前项目地址基准相差 1 | 改错会导致全部点位偏移 | 保持红线不变，现场读取已知寄存器确认 |
| SQLite 完整 7×24 尚未执行 | 不能直接宣称稳定性最终通过 | 在实际生产环境记录持续运行结果 |
| SunnyUI 3.7.2 为 GPL-3.0 | 商业闭源交付可能有许可证义务 | 项目方做许可证合规确认 |
| 当前目录不是 Git 仓库 | 无变更历史、回滚与提交追踪 | 建议初始化仓库或放回正确 clone 目录 |
| 7×24 尚未有效完成 | 稳定性出口未满足 | 在禁止休眠的工控机执行 |

## 7. 最终验收出口

1. GitHub PLC 目标提交已锁定，差异表与确认记录齐全。
2. Release 0 警告 0 错误；单元、SQLite 仓储测试和实际 PLC 主流程、异常验证通过。
3. 代码、配置和部署不包含 MySQL 运行依赖。
4. 全部可视业务控件使用 SunnyUI，实机 UI 与 DPI 验收通过。
5. 500 批与连续 7×24 达到零丢失、零错位、无泄漏趋势。
6. 现场 PLC 主链路通过，Offset/WordOrder/字节序/编码固化并签字。
7. 文档与最终代码一致，SQLite 备份恢复和故障处理可执行。
